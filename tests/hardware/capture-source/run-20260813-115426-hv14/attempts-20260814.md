# HV-14 실장비 실행 기록 — 2026-08-14

## 환경

- EOS 700D 1대, Canon EDSDK 13.19.0
- 시작 전·종료 후 helper self-check: `camera-ready`
- source comparison mode: `ab`
- display sample mode: `off`
- Daylight preset, 현재 부스 장면 고정
- 후속 재시도에서는 카메라가 매 요청 후 `camera-ready`로 돌아온 것을 확인하고 10~20초 간격으로 촬영

## 회차 결과

| 회차 | sessionId | helper 요청 | 저장 capture | source 행 | 중단 원인 |
| --- | --- | ---: | ---: | ---: | --- |
| 1 | `session_000000000018cb8ae04f9a8320` | 2 | 1 | 3 | `capture-download-timeout` |
| 2 | `session_000000000018cb8b2718d32c74` | 6 | 5 | 15 | `camera-busy` |
| 3 | `session_000000000018cb8b7655e66b2c` | 12 | 11 | 33 | `capture-download-timeout` |

runbook의 중단 규칙에 따라 실패 뒤 촬영을 이어 붙이지 않았고, 각 세션을 그대로 보존했다.

## Source 판정

성공적으로 저장된 capture 17건에서 51개 route 행이 생성됐지만 승격된 source는 0건이었다.

| route | 표본 | 승격 | 결과 |
| --- | ---: | ---: | --- |
| `embedded-jpeg` | 17 | 0 | 전부 `orientation-unsupported` |
| `camera-paired-jpeg` | 17 | 0 | 전부 `orientation-unsupported` |
| `windows-shell-thumbnail` | 17 | 0 | 전부 `absent` |

Route B의 capability probe는 매회 descriptor를 읽었고 `rawPlusJpegSupported: true`였다. paired JPEG와
RAW는 동일 request/capture/group으로 도착했으며 fallback correlation은 사용되지 않았다.

## 판정

**HV-14 qualifying run: No-Go.** 35회 회차를 완결하지 못했고, 현재 구현에서 세 route 모두 실제
EOS 700D source로 승격되지 않았다. 시도 3의 기계식 completeness 결과는
`completeness/attempt3-source-completeness.md`에 FAIL로 보존했다.

다음 재실행 전 필요한 조치:

1. EOS 700D의 실제 orientation을 지원하거나 정규화한다.
2. Windows Shell source가 준비된 뒤 Route C를 측정하도록 시점을 보정한다.
3. `camera-ready` 이후에도 발생하는 `camera-busy`/RAW handoff timeout을 해결한다.
4. helper 단계에서 실패한 요청도 source 비교 분모와 실패 행에 남긴다.
