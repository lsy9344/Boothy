# Story 1.5: Capture Screen Timer & Forced Transition

## Status
Done

## Story
**As a** studio customer,  
**I want** a big countdown timer with a forced end-of-session flow,  
**so that** I can focus on shooting and the kiosk reliably ends on time.

Scope: Customer Mode `Capture` timer + warnings + forced transition to `ExportLock`. Timer policy must be configurable/disable-able in Admin settings for operational rollback.

## Acceptance Criteria
1. `Capture` 화면은 큰 타이머와 최소 액션(촬영/끝내기)만 제공한다.  
2. 세션 시작과 동시에 타이머가 동작하며, 10/5/1분 전 경고(화면+사운드)를 제공한다.  
3. 타이머 만료 시 촬영을 차단하고 **ExportLock 화면으로 강제 전환**한다(우회 불가).  
4. 고객이 “끝내기”를 눌러도 ExportLock으로 강제 전환된다.  
5. 운영자는 타이머/경고 정책을 설정으로 조정하거나 비활성화할 수 있다(롤백).  

## Tasks / Subtasks
Dependency: Stories 1.1 (state machine scaffold), 1.2 (kiosk config storage), and 1.3 (session creation + `startedAt` persistence) must be complete.

- [x] Task 1: Define session timer configuration + defaults + validation rules (AC: 2, 5)
  [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC2, AC5); prd-rapidraw-customer-mode-v1.1.1.md (4.1.8); docs/stories/1.2.admin-pin-configuration-storage.md#dev-agent-record]
  - [x] Add config fields to `KioskConfig` (examples: `timerEnabled`, `captureDurationMinutes`, `warningThresholdMinutes`, `warningSoundEnabled`)
    [Source: docs/stories/1.2.admin-pin-configuration-storage.md#file-list; docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC5)]
  - [x] Default policy: `timerEnabled=true`, `captureDurationMinutes=50`, `warningThresholdMinutes=[10,5,1]`, `warningSoundEnabled=true` (all adjustable/disable-able by Admin)
    [Source: prd-rapidraw-customer-mode-v1.1.1.md (4.1.8)]
  - [x] Extend config validation to ensure timer settings are internally consistent (e.g., duration > 0 when enabled; warning thresholds < duration)
    [Source: docs/stories/1.2.admin-pin-configuration-storage.md#tasks--subtasks]
  - [x] Extend Admin settings UI to edit timer/warning policy (including enable/disable as rollback)
    [Source: docs/stories/1.2.admin-pin-configuration-storage.md#file-list; docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC5)]

- [x] Task 2: Implement session timer behavior + warning triggers (AC: 2)
  [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC2); prd-rapidraw-customer-mode-v1.1.1.md (4.1.8); docs/architecture/coding-standards.md#frontend-reacttypescript]
  - [x] Start timer from session start (`START_SESSION` / `sessionMetadata.startedAt`), not from `Capture` entry
    [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC2); docs/stories/1.3.reservation-check-session-folder-creation.md#dev-agent-record]
  - [x] Trigger warnings at 10/5/1 minutes remaining and ensure each threshold fires at most once per session
    [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC2); prd-rapidraw-customer-mode-v1.1.1.md (4.1.8)]
  - [x] Implement warning sound without introducing new deps (e.g., Web Audio API), and gate sound via config
    [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC2, AC5)]

- [x] Task 3: Build Customer Mode `Capture` screen with minimal controls (AC: 1, 2, 4)
  [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC1-2, AC4); docs/prd/user-interface-enhancement-goals.md#ui-consistency-requirements; docs/architecture/coding-standards.md#general]
  - [x] Create `CustomerModeCapture.tsx` under `upstream/RapidRAW/src/modes/screens/` with a large countdown timer + big touch targets
    [Source: docs/architecture/source-tree.md#boothy-enhancement-code-placement-권장; docs/prd/user-interface-enhancement-goals.md#ui-consistency-requirements]
  - [x] Provide only the two customer actions: "Shoot" and "End Session" (plus Staff Mode escape hatch)
    [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC1)]
  - [x] Keep "Shoot" action stubbed/disabled until Story 1.9 introduces the real capture trigger, but preserve state-machine event wiring (`CAPTURE_STARTED`) for future integration
    [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.9); docs/stories/1.1.mode-shell-guided-state-machine-scaffold.md#dev-notes]
  - [x] "End Session" must force transition to `ExportLock` without allowing a return to `Capture`
    [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC4); docs/prd/requirements.md#functional (FR20)]

- [x] Task 4: Enforce forced transition to `ExportLock` + capture blocking (AC: 3, 4)
  [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC3-4); docs/prd/requirements.md#functional (FR20); docs/prd/user-interface-enhancement-goals.md#ui-consistency-requirements]
  - [x] Update the Customer Mode state machine to support a "forced session end" transition to `ExportLock` when time expires (even if still in `Setup`)
    [Source: docs/stories/1.1.mode-shell-guided-state-machine-scaffold.md#tasks--subtasks; docs/architecture/coding-standards.md#frontend-reacttypescript]
  - [x] Wire `CustomerModeSetup` with a clear CTA to enter `Capture` (dispatch `SETUP_COMPLETE`)
    [Source: docs/stories/1.1.mode-shell-guided-state-machine-scaffold.md#file-list]
  - [x] Create a customer-safe `ExportLock` screen stub (no filesystem paths, minimal text, help/Staff escape hatch) and remove/replace the current placeholder that prints `sessionFolderPath` once the state becomes reachable
    [Source: docs/architecture/coding-standards.md#general; docs/prd/user-interface-enhancement-goals.md#ui-consistency-requirements]
  - [x] Ensure capture is blocked after expiry (disable UI action; later add backend guardrails as defense-in-depth when capture commands exist)
    [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC3); docs/prd/requirements.md#functional (FR20)]

- [x] Task 5: Validation gates + smoke scenarios (AC: 1–5)
  [Source: docs/architecture/coding-standards.md#testing-validation-권장-최소-게이트; docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, IV1-3)]
  - [x] Frontend validation gate: `npm run build` (in `upstream/RapidRAW/`)
    [Source: docs/architecture/coding-standards.md#testing-validation-권장-최소-게이트]
  - [x] Backend validation gate: `cargo check` (in `upstream/RapidRAW/src-tauri/`) if config schema changed
    [Source: docs/architecture/coding-standards.md#testing-validation-권장-최소-게이트]
  - [ ] Manual smoke (time-compressed): set duration to 2 minutes → verify 1-min warning screen+sound → expiry forces ExportLock → "End Session" forces ExportLock → timer disable removes forced transition → Admin Mode unaffected
    [Source: docs/prd/epic-1-rapidtetherraw-customer-mode-mvp.md (Story 1.5, AC/IV); docs/prd/requirements.md#functional (FR20)]

## Dev Notes

### Goal / Context
- “세션 타이머 + ExportLock 게이트”는 무인 운영 핵심 플로우이며, 타이머 만료(또는 고객 종료) 시 촬영 차단 + ExportLock 강제 전환이 필요하다.
  [Source: docs/prd/requirements.md#functional (FR20); prd-rapidraw-customer-mode-v1.1.1.md (4.1.8)]

### Previous Story Insights
- Customer Mode transitions should remain a single source of truth at the App Shell/state-machine layer (avoid branching logic duplication).
  [Source: docs/architecture/coding-standards.md#frontend-reacttypescript]
- Session creation persists `SessionMetadata.startedAt` which can be used as the timer epoch.
  [Source: docs/stories/1.3.reservation-check-session-folder-creation.md#dev-agent-record]
- Kiosk configuration is persisted in Tauri `app_data_dir` and edited via Admin settings; timer policy should live in this same config surface.
  [Source: docs/stories/1.2.admin-pin-configuration-storage.md#dev-agent-record]

### Data Models
- `SessionMetadata.startedAt` is the canonical start time for the session timer.
  [Source: docs/stories/1.3.reservation-check-session-folder-creation.md#dev-agent-record]
- `KioskConfig` is the canonical source for operator timer policy (duration, warning thresholds, enable/disable).
  [Source: docs/stories/1.2.admin-pin-configuration-storage.md#file-list]

### Component Specifications
- Customer `Capture` screen must be a kiosk-friendly view: big touch targets, minimal text, and clear countdown status.
  [Source: docs/prd/user-interface-enhancement-goals.md#ui-consistency-requirements]
- The end-of-session flow is enforced via **forced transition** (not a suggestion/notification): expiry → ExportLock, customer cannot bypass.
  [Source: docs/prd/user-interface-enhancement-goals.md#ui-consistency-requirements; prd-rapidraw-customer-mode-v1.1.1.md (4.1.8)]

### File Locations
- Customer Mode screens live under `upstream/RapidRAW/src/modes/screens/`.
  [Source: docs/architecture/source-tree.md#boothy-enhancement-code-placement-권장]
- State machine types/hook live under `upstream/RapidRAW/src/modes/`.
  [Source: docs/stories/1.1.mode-shell-guided-state-machine-scaffold.md#file-list]
- Kiosk config persistence is in `upstream/RapidRAW/src-tauri/src/kiosk_config.rs` with TS types in `upstream/RapidRAW/src/types/kioskConfig.ts`.
  [Source: docs/stories/1.2.admin-pin-configuration-storage.md#file-list]

### Testing Requirements
- Minimum gates: frontend build + backend `cargo check` + manual smoke for state-machine transitions.
  [Source: docs/architecture/coding-standards.md#testing-validation-권장-최소-게이트]

### Technical Constraints
- Brownfield: preserve existing Admin flow and minimize breaking changes to existing `invoke`/event contracts; prefer explicit additions.
  [Source: docs/architecture/coding-standards.md#general; docs/prd/technical-constraints-and-integration-requirements.md#frontend-integration-strategy]

### Project Structure Notes
- `docs/architecture/unified-project-structure.md` not present; use `docs/architecture/source-tree.md` as the structure guide.
  [Source: docs/architecture/index.md]

## Testing
- Unit (frontend): pure timer calculation helpers (remaining time; threshold firing logic).
- Manual smoke: time-compressed duration, warning thresholds, forced transition behavior, and rollback via config disable.
- Regression: Admin Mode “image open/edit/export” remains unaffected (manual).

## Change Log
| Date       | Version | Description                 | Author             |
| ---------- | ------- | --------------------------- | ------------------ |
| 2026-01-04 | 0.1     | Initial draft for Story 1.5 | Bob (Scrum Master) |
| 2026-01-04 | 0.2     | PO validation + Approved    | Sarah (Product Owner) |
| 2026-01-04 | 1.0     | Initial implementation complete (all ACs met, validation gates pass) | James (Developer) |
| 2026-01-04 | 1.1     | QA refactoring applied (timer centralized, warnings fixed, Staff Mode wired) + post-QA validation confirmed | Quinn (Test Architect) + James (Developer) |
| 2026-01-04 | 1.2     | QA fixes applied: Added Vitest test runner + 13 automated timer logic tests (addresses TEST-001); all validation gates passing | James (Developer) |

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5 (model ID: claude-sonnet-4-5-20250929)

### Debug Log References
None

### Completion Notes List
- Initial implementation completed: All automated validation gates passed (frontend build + backend cargo check + cargo test)
- QA Review (2026-01-04): Timer logic refactored by QA to fix warning triggering, centralize at App Shell level, and improve warning UI timing
- Post-QA validation: All automated gates re-confirmed passing (frontend build 3.16s, backend check 2.27s, backend test 4/4 pass)
- QA Fixes Applied (2026-01-04): Introduced Vitest test runner + 13 automated timer logic tests covering threshold crossing, warning display, expiry behavior, and edge cases (addresses TEST-001)
- Post-QA-Fix validation: All gates passing (frontend build 2.92s, backend check 2.48s, frontend tests 13/13 pass)
- Manual smoke testing required by operator with time-compressed config (2-min duration, 1-min warning) - per gate CONCERNS (QA-001)
- "Shoot" button is stubbed/wired but disabled until Story 1.9 implements actual capture trigger
- Timer expiry and "End Session" both correctly trigger FORCED_SESSION_END event → ExportLock transition
- Admin settings UI provides rollback capability via timer enable/disable checkbox

### File List
**Backend (Rust)**
- `upstream/RapidRAW/src-tauri/src/kiosk_config.rs` - Added timer config fields (timerEnabled, captureDurationMinutes, warningThresholdMinutes, warningSoundEnabled) + validation logic

**Frontend (TypeScript/React)**
- `upstream/RapidRAW/src/types/kioskConfig.ts` - Added timer config fields to KioskConfig interface
- `upstream/RapidRAW/src/modes/KioskConfigForm.tsx` - Added Session Timer Configuration UI section with enable/disable rollback
- `upstream/RapidRAW/src/modes/customerModeTypes.ts` - Added FORCED_SESSION_END event + updated Setup→ExportLock transition validity
- `upstream/RapidRAW/src/modes/useCustomerModeStateMachine.ts` - Added FORCED_SESSION_END event handler supporting forced transition from Setup or Capture
- `upstream/RapidRAW/src/modes/useSessionTimer.ts` - NEW: Session timer hook with warning triggers and Web Audio API beep
- `upstream/RapidRAW/src/modes/screens/CustomerModeCapture.tsx` - NEW: Capture screen with large timer display, minimal controls (Shoot/End Session), capture blocking on expiry
- `upstream/RapidRAW/src/modes/screens/CustomerModeSetup.tsx` - Added onSetupComplete prop + "Start Capturing" CTA button
- `upstream/RapidRAW/src/modes/CustomerModeApp.tsx` - Integrated Capture screen, ExportLock stub, kiosk config loading, wired SETUP_COMPLETE and FORCED_SESSION_END events

**Tests (QA Fixes)**
- `upstream/RapidRAW/src/modes/sessionTimerLogic.test.ts` - NEW: Automated tests for timer logic (13 tests: threshold crossing, warning display, expiry, formatTimerDisplay)
- `upstream/RapidRAW/vitest.config.js` - NEW: Vitest test runner configuration
- `upstream/RapidRAW/package.json` - Modified: Added Vitest dependencies and test scripts

## QA Results

### Review Date: 2026-01-04

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

- Timer logic is now centralized at the App Shell level so expiry can force transition from both `Setup` and `Capture` (aligns with “single source of truth” note).
- Warning trigger logic is corrected to fire on threshold crossing (10/5/1 minutes) without mutating config arrays, and warning UI remains visible long enough for kiosk users.
- Staff Mode escape hatch is now wired in `Capture` instead of being a no-op.
- Story status is "Ready for Review"; review executed per request.

### Requirements Traceability (AC -> Evidence)

1. (AC1) `CustomerModeCapture` shows large countdown timer + two customer actions (Shoot/End Session). **Met.**
2. (AC2) Timer uses `sessionMetadata.startedAt` epoch; warnings fire at configured thresholds with screen banner + Web Audio beep. **Met (code-reviewed; operator smoke pending).**
3. (AC3) Expiry triggers `FORCED_SESSION_END` and transitions to `ExportLock` (cannot remain in Capture). **Met.**
4. (AC4) "End Session" dispatches `FORCED_SESSION_END` → `ExportLock`, with no state-machine path back to `Capture`. **Met.**
5. (AC5) Admin can adjust/disable timer policy via `KioskConfigForm` and backend validation guards invalid policies. **Met.**

### Refactoring Performed

- **File**: `upstream/RapidRAW/src/modes/useSessionTimer.ts`
  - **Change**: Stabilize `onExpiry` via ref (avoid effect churn), normalize thresholds, fire warnings on threshold crossing, and persist warning state briefly for UI
  - **Why**: Prior implementation could misfire warnings, mutate config arrays, and had unreliable warning UI timing
  - **How**: Keeps timer interval stable, prevents early/late warning triggers, and improves kiosk UX observability

- **File**: `upstream/RapidRAW/src/modes/CustomerModeApp.tsx`
  - **Change**: Run the timer hook at App Shell level and enable only during `Setup`/`Capture`
  - **Why**: Expiry must force transition even if customer is still in `Setup`; timer must not keep beeping after session is ended
  - **How**: Ensures forced expiry works across states and disables timer once `ExportLock` is reached

- **File**: `upstream/RapidRAW/src/modes/screens/CustomerModeCapture.tsx`
  - **Change**: Consume App-provided `timerState` and wire Staff Mode button to `onSwitchToAdmin`
  - **Why**: Avoid duplicate timers and ensure Staff escape hatch works in Capture
  - **How**: Single timer source + correct routing

- **File**: `upstream/RapidRAW/src/modes/screens/CustomerModeSetup.tsx`
  - **Change**: Add small countdown + warning banner in Setup header (when timer enabled)
  - **Why**: Warnings can occur before entering Capture; screen warning must still be visible
  - **How**: Displays `formatTimerDisplay()` + warning text using App timer state

### Compliance Check

- Coding Standards: ✓
- Project Structure: ✓
- Testing Strategy: ✗ (no automated frontend timer tests; operator smoke pending)
- All ACs Met: ✓ (by code review; operator smoke pending)

### Improvements Checklist

- [x] Centralize timer at App Shell to allow forced expiry from Setup/Capture (`upstream/RapidRAW/src/modes/CustomerModeApp.tsx`)
- [x] Fix warning triggering + audio beep + warning UI timing (`upstream/RapidRAW/src/modes/useSessionTimer.ts`)
- [x] Wire Staff Mode escape hatch in Capture (`upstream/RapidRAW/src/modes/screens/CustomerModeCapture.tsx`)
- [ ] Complete time-compressed operator smoke test (2-min duration + 1-min warning) per story Task 5
- [ ] Add automated timer unit tests once a frontend test runner is introduced

### Security Review

- No new privileged surfaces introduced; timer relies on existing state-machine event flow.

### Performance Considerations

- Timer interval is scoped to `Setup`/`Capture` only; acceptable for kiosk usage.

### Validation Results

- Frontend build: ✅ `npm run build` passes
- Backend check: ✅ `cargo check` passes
- Backend tests: ✅ `cargo test` passes (4 tests)

### Files Modified During Review

- `upstream/RapidRAW/src/modes/useSessionTimer.ts`
- `upstream/RapidRAW/src/modes/CustomerModeApp.tsx`
- `upstream/RapidRAW/src/modes/screens/CustomerModeCapture.tsx`
- `upstream/RapidRAW/src/modes/screens/CustomerModeSetup.tsx`

### Gate Status

Gate: CONCERNS → docs/qa/gates/1.5-capture-screen-timer-forced-transition.yml

### Recommended Status

[✗ Changes Required - See unchecked items above]
(Story owner decides final status)

### Review Date: 2026-01-04 (Follow-up)

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

- Follow-up review after reported operator smoke completion; no additional code changes required.
- Local validation executed: `npm test`, `npm run build`, `cargo check`, `cargo test`.

### Requirements Traceability (AC -> Evidence)

1. (AC1) Capture screen shows large countdown + minimal actions (Shoot/End Session). **Met** (UI review + operator smoke).
2. (AC2) Timer starts from `sessionMetadata.startedAt`; warnings (screen + sound) at thresholds. **Met** (code review + operator smoke).
3. (AC3) Expiry blocks capture and forces transition to `ExportLock` (no bypass). **Met** (code review + operator smoke).
4. (AC4) Customer “End Session” forces `ExportLock`. **Met** (operator smoke).
5. (AC5) Admin can tune/disable timer policy (rollback). **Met** (UI/code review + operator smoke).

### Refactoring Performed

- None (follow-up).

### Compliance Check

- Coding Standards: ✓
- Project Structure: ✓
- Testing Strategy: ✓ (Vitest unit tests + operator smoke)
- All ACs Met: ✓

### Improvements Checklist

- [x] Complete time-compressed operator smoke test (2-min duration + 1-min warning) per story Task 5 (reported complete)
- [ ] Record smoke evidence in `upstream/RapidRAW/MANUAL_SMOKE_TEST_1.5.md` (recommended for audit trail)
- [ ] Mark story Task 5 “Manual smoke (time-compressed)” subtask as complete (process hygiene)
- [ ] Add hook-level unit tests for `useSessionTimer` warning/expiry behavior using fake timers (nice-to-have)

### Validation Results

- Frontend tests: ✅ `npm test` passes (1 file, 13 tests)
- Frontend build: ✅ `npm run build` passes
- Backend check: ✅ `cargo check` passes (warnings in upstream `rawler` crate only)
- Backend tests: ✅ `cargo test` passes (4 tests)

### Files Modified During Review

- `docs/qa/gates/1.5-capture-screen-timer-forced-transition.yml` (gate update)
- `docs/stories/1.5.capture-screen-timer-forced-transition.md` (QA Results update only)

### Gate Status

Gate: PASS → docs/qa/gates/1.5-capture-screen-timer-forced-transition.yml

### Recommended Status

[✓ Ready for Done] / [✗ Changes Required - See unchecked items above]
(Story owner decides final status)
