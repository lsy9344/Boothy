# Source Drafts

This folder contains older/reference story drafts that were used to derive the canonical implementation stories in `docs/stories/`.

They are kept for traceability, but are **not** the primary documents to implement against.

