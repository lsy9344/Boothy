# Risk Profile: Story 1.6 — Export Rules & Filename Templating
Date: 2026-01-04  
Reviewer: Quinn (Test Architect)

## Summary
Overall risk: **Medium** (Gate: **CONCERNS**)

## Risk Register

| ID | Area | Risk | Likelihood (1-5) | Impact (1-5) | Score |
| --- | --- | --- | --- | --- | --- |
| REQ-003 | Requirements | Story AC2 default-template literal (`휴대폰뒤4자리-{hh}시-{sequence}`) is misaligned with implementation/default (`{phoneLast4}-{hh}시-{sequence}`), risking acceptance ambiguity and operator confusion. | 4 | 3 | 12 |
| TEST-002 | Quality | No automated “output correctness” test asserts session export produces valid JPEG outputs and applies template end-to-end, increasing regression risk. | 3 | 3 | 9 |

## Mitigations
- REQ-003: Decide whether AC2 string is literal vs intent; either add alias support for `휴대폰뒤4자리` or update story/PRD/UI copy to `{phoneLast4}` (PO decision).
- TEST-002: Add a minimal backend test that runs the export pipeline on a small known image and verifies output bytes (JPEG signature) + naming/template application.

## Update (Final Re-review)
Reviewed: 2026-01-04  
Result: **Low** (Gate: **PASS**)

- REQ-003: **Resolved** — templating supports both `{phoneLast4}` and the literal `휴대폰뒤4자리` alias (`generate_filename_from_template_with_phone`).
- TEST-002: **Resolved** — session export has a smoke-level output correctness test asserting valid JPEG bytes + template naming (`test_session_export_output_jpeg_and_template`).

### Residual Risks (Monitor)
- Template placeholder validation in `KioskConfig.validate()` is intentionally lightweight; consider tightening to catch typos early (low likelihood/impact).
