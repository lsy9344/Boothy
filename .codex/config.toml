# Hook fallback instructions are preserved here for quick re-enable.
# developer_instructions = """
# In this repository, use history/camera-capture-validation-history.md as the canonical running log for the camera capture troubleshooting flow.
# Before you finish each user turn, run relevant verification commands for your implementation or investigation, report the results to the user, and record cause analysis, attempts, implementation, verification, and test results in that history document.
# If no verification command applies, explicitly explain why in both the response and the history document.
# Follow this workflow even when lifecycle hooks do not fire. The official hooks page notes that native Windows Codex CLI currently disables hooks.
# """

[features]
multi_agent = true
codex_hooks = false

[agents]
max_threads = 6
max_depth = 1
job_max_runtime_seconds = 1800
