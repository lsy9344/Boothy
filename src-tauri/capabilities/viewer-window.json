{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "viewer-window",
  "description": "Read-only customer viewing surface. Without this capability the viewer webview has zero IPC permissions.",
  "windows": ["viewer-window"],
  "permissions": ["core:default"]
}
